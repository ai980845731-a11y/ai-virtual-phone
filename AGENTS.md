# AI 交互规范

本文件定义 AI 助手在本项目中的开发交互行为准则。

---

## 报错与异常处理

当用户遇到技术报错时（Git 冲突、构建失败、API 错误、部署问题等）：

1. **先用中文解释报错的实际含义**——不要直接贴原始英文错误信息，翻译成用户能理解的话
2. **说明是否需要用户操作**——如果 AI 能自行处理，明确告知"你不需要操作，我来处理"
3. **给出最小下一步操作**——只告诉用户需要做什么，不展开多余的技术背景
4. **不直接输出英文错误翻译**——不要把 git/CI/API 的英文原文原样丢给用户，即使加了翻译也不够，应该用人话重述
5. **不假设用户理解 Git/API/部署术语**——fast-forward、merge conflict、HTTP 429、token limit 等术语需要解释，不能默认用户知道

### 示例

**错误做法：**
> "提交失败，test 分支不是 fast-forward。请先拉取最新 test 分支，基于最新版本重新应用修改。"

**正确做法：**
> "提交失败——test 分支在我上次读取之后有了新的提交，和我的改动冲突了。你不需要操作，我来同步最新版本重新提交。"

---

## 代码修改规范

- 修改前先读取当前内容，基于真实代码再动手
- 不确定的功能/设置明确说不确定，不编造
- 涉及角色卡、世界书、预设、正则、桌面美化的创作需求，引导用户找桌面 AI 助手「小卷」
